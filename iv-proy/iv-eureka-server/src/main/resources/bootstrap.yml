spring:
  application:
    name: iv-eureka-server
  cloud:
    config:
      uri: ${CONFIG_SERVER_URI:http://localhost:8888}
      username: ${CONFIG_SERVER_USERNAME:iv-admin}
      password: ${CONFIG_SERVER_PASSWORD:iv-admin}