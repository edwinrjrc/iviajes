spring:
  application:
    name: iv-gateway
  config:
    import: "optional:configserver:http://localhost:8888" # Esto reemplaza la uri antigua
  cloud:
    config:
      username: iv-admin
      password: iv-admin
      fail-fast: true